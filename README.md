🛡️ Web Vulnerability Scanner

A web-based tool to scan websites for common security vulnerabilities. Built with Flask, HTML/CSS, and JavaScript, it supports single URL scans and batch scans with results shown in a transparent UI box.

🔹 Features

Single URL scan

Batch URL scan (one URL per line)

Scan results displayed inside a transparent box

Dynamic input fields for single scan

⚡ Installation

git clone https://github.com/upasana1927/WEB-VULNERABLITY-SCANNER.git

cd WEB-VULNERABLITY-SCANNER

pip install -r requirements.txt

python app.py


Open http://127.0.0.1:5000 in your browser.

📁 Structure

templates/   # HTML pages

static/      # Images

app.py       # Flask app

requirements.txt
